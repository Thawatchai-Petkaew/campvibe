# Lessons Ledger — Scout team continuous learning

Append-only record of lessons distilled from **closed** stories by the `retro` skill (`/retro <CAM-###>`). The ledger is the audit trail, the dedupe source, and the prune driver. Lessons that are reusable + role-general get **promoted** into `.claude/rules/<role>.md` (a `## Common Rationalizations` row or a `## Standards` bullet) after **owner approval** — that is what makes the Scout sub-agents read them before their next task (Iron Rule #4).

**How to use this file**

- One row per lesson. Newest at the top of the table.
- `Status`: `proposed` (in the ledger, not yet in a rule) → `promoted` (owner-approved, now in the named rule) → `pruned` (merged/retired during a prune pass; keep the row for history).
- Before adding a row, scan for a near-duplicate → **strengthen/merge** the existing one instead of twinning it.
- `Destination`: the rule file it landed in, or `memory` (orchestrator), `docs/context`, `DESIGN.md`, or a skill — per the `retro` routing rules.
- Every promoted lesson cites its origin CAM ticket (the WHY) so a future reader knows why the rule exists.

## Ledger

| Date | CAM | Role | Type | Lesson (mistake → better rule) | Destination | Status |
| 2026-07-18 | CAM-400/398 | frontend, backend | process | Source changes broke sibling source-inspection pins that surfaced only in a LATER full run (cam-302, cam-194). → Before handoff: grep `__tests__/` for every source string the diff changes + re-run the full suite as the LAST act after the final edit. | `.claude/rules/code.md` | promoted |
| 2026-07-18 | CAM-400 | architect, backend | process | Capacity seam shipped divergent zero-semantics (write gate 0=unbounded vs display 0=full) across 3 stories that each "kept it by construction" → deterministic overbooking. → A seam sweep ends in ONE invariant + a cross-layer invariant test; divergent edge-value semantics is a defect in scope now. | `.claude/rules/architecture.md` §15b | promoted |
| 2026-07-18 | CAM-396 | frontend, backend | process | Client gate read a server-rendered `isLoggedIn` prop → stale after modal login (router.refresh race) → first press failed. → An auth/permission gate in a client component reads the LIVE client session (`useSession()`), never a server-snapshot prop. | `.claude/rules/code.md` | promoted |
| 2026-07-18 | CAM-396 | qa, product-owner | process | QA verified both static states of a state-dependent gate but not the login→retry TRANSITION, where it broke. → Test the transition path; browser-only transitions become explicit owner-verify AC rows at spec time. | `.claude/rules/qa.md` | promoted |
| 2026-07-18 | CAM-396 | orchestrator | process | First run of the JSON return discipline was only partially followed (prose returns accepted). → The orchestrator bounces a non-conforming return once before accepting; compliance is an acceptance check. | `.claude/rules/efficiency.md` | promoted |
|---|---|---|---|---|---|---|
| 2026-07-26 | CAM-512 | analyst/orchestrator | process | The AI-chat misses we'd been patching with concept-map hacks (มือใหม่/จิบเบียร์/เล่นน้ำ) were ALL designed in the original v1 product spec (Google Sheet) — 13 option groups designed, only 7 built. We reinvented what already existed. → "existing-data-first" must check the ORIGINAL product spec / design docs, not just the current schema, before researching or suggesting new data. | `.claude/rules/discovery.md` + `ai-chat-improve` skill | promoted |
| 2026-07-26 | CAM-515 | architect/frontend | process | Adding the FIRST dedicated (non-merged) filterable MasterData group wired FilterModal but the chip produced a query param the Home grid silently ignored — the catalog fetch pipeline (page.tsx→CatalogResults→InfiniteScrollGrid→catalog-cursor validation→/api/campsites GET) also needs the param, plus BOTH AI tools (search + bulk-availability pin their own code consts), host form (6 touchpoints), and the detail section. → A new filterable MasterData group's blast radius is the WHOLE catalog pipeline + both AI tools + host form + detail — enumerate all of it, not just the filter modal. | `.claude/rules/architecture.md` | promoted |
| 2026-07-26 | CAM-519 | devops/orchestrator | process | A fix was committed LOCALLY but not pushed before polling CI → the guardrail gate ran the stale remote branch and failed confusingly; the local eval (with the fix) was green. → After any fix, `git push` BEFORE polling CI / re-reading a gate result — CI runs the REMOTE branch, never the local commit. | `.claude/rules/ops.md` | promoted |
| 2026-07-26 | CAM-513→519 | qa | process | Adding the SEA terrain code made the eval GUARDRAIL case "ริมทะเล"→expected BEAC stale (the model correctly now picks SEA); it surfaced only at a later slice's eval and flipped guardrail below 100%. → Adding a taxonomy code must SWEEP the golden/guardrail cases that pin the old best-match for the words that code now serves (a data-add can invalidate a behavioral expectation, like the two-guards count trap but for the matched value). | `.claude/rules/qa.md` | promoted |
| 2026-07-26 | CAM-513/514 | orchestrator | process | Specs authored in the MAIN tree were orphaned AGAIN (recurrence of CAM-506/509) because builders branch off origin/dev without them — only the builder that copied its own spec shipped it. Fixed mid-epic by creating the feature branch + committing the spec FIRST, then dispatching the builder onto that branch. → Commit the story spec to the feature branch (or have the builder author it in-worktree) BEFORE dispatch; a spec in the orchestrator's uncommitted main tree never rides the PR. | `.claude/rules/efficiency.md` (dispatch) | promoted |
| 2026-07-26 | CAM-517 | backend/analyst | data | The host form models `campSiteType` as a multi-select ARRAY + zod `z.array`, but the DB column is a scalar String — the write path silently coerces to `[0]`, dropping extra selections (and an empty array writes an invalid "CAMPGROUND" sentinel). → A field's validation + form cardinality MUST match the column cardinality; a multi-select over a scalar column silently truncates. Match the form to the storage (or make storage a relation) — never bridge with a silent `[0]`. | `.claude/rules/code.md` | promoted |
| 2026-07-26 | CAM-512 | devops | process | Staging = Prisma-Postgres POOLED url (`pooled.db.prisma.io`, no direct url in the backup); a bulk `prisma db seed` transiently crashed ("server conn crashed") then SUCCEEDED on retry. → A pooled-proxy connection crash on a heavy seed can be transient — retry ONCE before concluding it's blocked; staging data refresh = `prisma db seed` (MasterData) THEN `db:load:staging` (loader skips unknown codes, so seed first), both idempotent-upsert/never-deleteMany/prod-guarded. | memory (project-status) | promoted |
| 2026-07-25 | CAM-500 | qa | process | De-anchoring the `province` param (removed the "Chiang Mai" example) passed a static diff read but made the model DROP a user-named province — "แคมป์ริมน้ำเชียงใหม่" returned southern camps mislabeled as Chiang Mai; caught by the OWNER, not tests. → A prompt/LLM-behavior change is verified BEHAVIORALLY (a `strictParams` golden case + a real-endpoint reproduction), never by a diff read alone — a static check can't see a behavioral regression. | `.claude/rules/qa.md` | promoted |
| 2026-07-25 | CAM-501/503 | code/backend | data | Thai substring/lexicon matching over user text false-matched common words — `ใต้ต้นไม้`→ภาคใต้, `ตากแดด`→จ.ตาก (DEF-1); `เขาใหญ่` collided with เขา+ใหญ่ (DEF-2). Both caught by QA Prove-It. → Any substring/lexicon match over Thai input needs a guard (boundary/`ภาค`-prefix/ambiguous-name skip-set/camping-context marker) + a red regression proving the false-match BEFORE the fix. | `.claude/rules/code.md` | promoted |
| 2026-07-25 | eval-harness | qa | process | The AI golden-eval dispatch mock returned empty `data: {}` for every tool, starving the search→check chain — a single-turn "is ‹named camp› available?" could never reach `checkAvailability` (needs a uuid) inside the harness, so 3 cases counted as fails for a HARNESS artifact, not a product bug. → A harness mock must return a chainable/reachable shape for any multi-step flow; when an expected outcome is unreachable given the mock/seed state, that's a harness defect (fix fidelity), not a model miss — verify reachability before reading the correctness %. Pairs with the CAM-460 `seededShownResults` reachability rule. | `.claude/rules/qa.md` | promoted |
| 2026-07-25 | CAM-497 | devops | data | `db:sync-from-staging`'s copy list named only models, silently dropping the implicit `_CampSiteToMasterData` m2m join table → synced camps had 0 options and Home filters returned 0 in production. → A Prisma data-sync/copy list must include implicit m2m join tables (`_A_to_B`, A=owning id, B=related), truncated+copied in FK order, and be verified by row-count after sync. | `.claude/rules/ops.md` | promoted |
| 2026-07-25 | CAM-504 | orchestrator/devops | process | The branch was deleted (local+remote) and the story marked Done in the SAME batch as a merge that was actually BLOCKED (BEHIND after a concurrent merge); recovered only because the commit was still in the object store. → (strengthen the CAM-203 row) never delete a branch or mark a story Done in the same batch as its merge — gate all cleanup on a confirmed `gh pr view --json state` = MERGED. | `.claude/rules/ops.md` | promoted |
| 2026-06-28 | CAM-242 | frontend/code | process | Stale-navbar (login/logout/avatar didn't reflect) was patched 3× — CAM-239 `update()`+`router.refresh()`, CAM-240 `navUser` status-gating, CAM-241 client `signIn`/`signOut` — each half-fixed it before the root was found by reading the next-auth source: `<SessionProvider>` had no initial `session` → `useSession()` starts `status="loading"` every load → the navbar arbitrated between a frozen server prop and a client session resolving later. → When a fix doesn't hold and the same symptom returns, read the library/framework's actual state machine (the `node_modules` source) for the root before stacking another symptom patch. | `.claude/rules/code.md` | promoted |
| 2026-06-28 | CAM-242 | frontend | code | Server-action `signIn`/`signOut` set the cookie server-side but never updated the client `SessionProvider`; combined with an un-hydrated provider, the navbar was stale on every page except the one client page that read `useSession()` itself. → NextAuth v5 App Router: hydrate `<SessionProvider session={await auth()}>` in the root layout AND use the client `signIn`/`signOut` from `next-auth/react` for client-component auth (so the provider syncs). | `.claude/rules/code.md` | promoted |
| 2026-06-28 | CAM-240 | security | security | Widening the NextAuth `auth()` middleware matcher to cover `/api` (added for the COMING_SOON gate) also wrapped NextAuth's OWN routes `/api/auth/*` (signin/signout/session/csrf) → logout/re-login broke + session polling slowed. → Always EXCLUDE `/api/auth` in the matcher's negative-lookahead; a gate for data APIs must never wrap the auth routes (regression introduced by CAM-237). | `.claude/rules/security.md` | promoted |
| 2026-06-28 | CAM-239 | backend | code | The upload route fell back to `fs.writeFile(public/uploads)` when `BLOB_READ_WRITE_TOKEN` was missing → throws on Vercel's read-only serverless filesystem → surfaced as a mystery `500`, uploads dead on staging. → A local-FS write fallback is a localhost-only convenience; make it `NODE_ENV==='development'`-only and in production return a clear error (503), never a doomed write (use object storage). | `.claude/rules/api.md` | promoted |
| 2026-06-28 | CAM-237/239 | orchestrator | reference | Vercel: an env-var change (Blob token / COMING_SOON) does NOT apply to running deployments — must Redeploy (uncheck build cache); the production domain does not auto-switch to a new deploy (use "Promote to Production"); env must be on the Production scope; verify via curl smoke, not the deploy "Ready" status. | memory (prod-soft-launch-coming-soon) | promoted |
| 2026-06-27 | CAM-221 | devops | process | A grep guard (check-ds) catches forbidden STRINGS, not structural/role drift — ~90 drift instances passed it (correct tokens, wrong role / re-implemented). → Use AST/co-occurrence heuristics for structural rules; roll a new guard out **report-mode → clear the backlog to 0 → flip to blocking** (never ship a blocking guard with a non-zero backlog). | `.claude/rules/ops.md` | promoted |
| 2026-06-27 | CAM-230 | qa | process | Playwright `toHaveScreenshot` baselines are OS-specific (macOS `-darwin` vs CI `-linux`) → the first CI run shows baseline-missing/pixel-diff red. → Commit Linux baselines (or generate them in CI) AND make the visual/a11y job advisory (`continue-on-error` + non-required check) so cross-OS pixel diffs never block the merge gate; route diffs to review. | `.claude/rules/qa.md` | promoted |
| 2026-06-27 | CAM-226 | qa | process | A design-system refactor changes canonical Tailwind classes, so source-inspection tests that PIN the old class (grep an exact className across consumer files) go red — A1/A2/A3/B3 each had to update sibling guard tests. → Updating a pinning test to the new canonical class is CORRECT (not weakening); prefer asserting at the shared primitive/guard over duplicating exact-class greps across many consumer test files. | `.claude/rules/qa.md` | promoted |
| 2026-06-27 | CAM-218 | security | security | A statically-prerendered route under a per-request CSP nonce has ALL its scripts blocked: CSP3 ignores `'unsafe-inline'` once a `nonce-` source is present, but the static HTML is baked at build with no nonce while the middleware sets a fresh nonce per request → next-themes, providers, every client bundle silently never run (theme/i18n/interactivity dead, no error thrown). → Any route that must run client JS under a nonce CSP must render dynamically (`export const dynamic = 'force-dynamic'`) so the per-request nonce is stamped on its scripts; verify the `nonce` in the HTML matches the response CSP header. | `.claude/rules/security.md` | promoted |
| 2026-06-27 | CAM-218 | code | process | `useSearchParams()` in a component (Navbar) rendered on `not-found.tsx`/`error.tsx` CSR-bails during the static `/404` prerender; with a root `app/loading.tsx` present the page got stuck on the loading skeleton (the bailout showed as a never-resolving Suspense fallback). → Wrap any `useSearchParams()` user in `<Suspense>` on error/not-found pages; a root `loading.tsx` is an app-wide Suspense net — don't delete it to "fix" one page (it breaks the others). | `.claude/rules/code.md` | promoted |
| 2026-06-27 | CAM-218 | code | process | (strengthen the CAM-201 deployed-only row) Re-shipped two wrong fixes (use-client→server, then delete loading.tsx) by theorizing before seeing the real error. → For a deployed-only rendering bug, capture the ACTUAL error first — the `npm run build` prerender error, the Vercel runtime log, the response CSP/cache header — before changing code; a green local build can still hide a prerender/CSP failure. | `.claude/rules/code.md` | promoted |
| 2026-06-27 | CAM-215 | security | security | The open-redirect (CWE-601) fix first allowed any leading-`/` path, blocking only `//` and `/\` — a `/\t//evil.com` (control-char prefix) still slipped through. → A same-origin redirect allowlist must accept only a leading single `/` AND reject control chars (charCode < 0x20), not just `//`/`/\`. | `.claude/rules/security.md` | promoted |
| 2026-06-27 | CAM-215/216 | orchestrator | process | (confirmation, no new rule) SEC-A/SEC-B applied existing rules correctly (zod at the boundary, no PII in logs, bcrypt ≥12, ownership), and PR #227 going behind `staging` after #226 merged re-hit the CAM-203 "branch-behind → quality-gate expected → update branch + re-run" flow — both confirm the promoted rules hold. | ledger only | promoted |
| 2026-06-27 | CAM-212 | backend | process | Almost added a keyset cursor to a list endpoint, which would change the response shape (array → {items,cursor}) and break 3 flat-array consumers. → Before paginating a list endpoint, check its consumers; a bounded `take` (newest-first) closes the unbounded-scan risk non-breaking — only do a cursor/contract change if the FE is updated in the same story. | `.claude/rules/api.md` | promoted |
| 2026-06-27 | CAM-211 | security | security | A security audit recommended an OPERATOR/ADMIN role-gate on camp creation, but the product intentionally lets any user self-register as a host (`operatorId = self`); gating would have broken onboarding with no upgrade flow. → Verify the intended authz/business rule + existing flow before applying an audit's "add a role gate"; if the goal is anti-abuse, a rate-limit closes it without breaking a self-service flow. | `.claude/rules/security.md` | promoted |
| 2026-06-27 | CAM-209 | security | security | NextAuth v5 `authorize` has no way to return an HTTP 429, so rate-limiting the credentials flow is done by returning `null` inside `authorize` (surfaces as CredentialsSignin), or by wrapping the route. → Don't try to emit a 429 from `authorize`; rate-limit before the password compare and return null. | `.claude/rules/security.md` | promoted |
| 2026-06-27 | CAM-209/210 | architect | process | The rate-limiter used a module-level `Map` — on serverless that is per-instance: it resets on cold start and is not shared across concurrent instances, so the limit is best-effort only. → Never rely on in-process state for cross-instance correctness (rate-limit/counter/cache-as-truth); a shared store is required. Reusing the primary DB for a hot counter consumes the very quota it protects — weigh that vs an external KV / edge WAF. | `.claude/rules/architecture.md` | promoted |
| 2026-06-27 | CAM-212/223 | qa | process | An SSE/stream test drained for a FIXED time window then asserted the event arrived → flaked under CI load when the event landed after the window. → (strengthen the sleep→condition row) For async/streamed output, wait until the awaited event/substring appears (early-exit) and lean on a real terminal condition (stream self-close at MAX_MS) to keep teeth — never a fixed drain window. | `.claude/rules/qa.md` | promoted |
| 2026-06-27 | CAM-213 | code | process | Almost removed unsplash as a floating `next.config` tweak; a full lifecycle grep found the refs lived in seed + 4 component fallbacks (+ a latent seed scalar-vs-relation mismatch), not the config. → For a "remove an external host/dependency everywhere" change, grep the whole lifecycle (model→write→read→seed→config→CSP→tests) before editing — the real surface is rarely just the obvious spot. | `.claude/rules/code.md` | promoted |
| 2026-06-27 | CAM-208 | orchestrator | process | (confirmation, not a new rule) The promoted skepticism/verify lessons held across the whole rate-limit epic: deferred the role-gate pending a business rule, questioned the Upstash dep (cost rule), scoped pagination non-breaking, mapped the image lifecycle before a sweeping change, and re-verified Done via MCP `get_issue` rather than trusting linear-sync's ambiguous "already in sync". | memory (confirmation) | promoted |
| 2026-06-27 | CAM-191 | orchestrator | process | Hotfix CAM-191 + the CAM-96..104 Atomic Schema epic were created without a project/epic → orphaned /status, had to be retro-grouped. → Every tracked issue gets a project (+ parent epic for a story) AT CREATION. | `.claude/rules/discovery.md` | promoted |
| 2026-06-27 | — | orchestrator | reference | Linear MCP + linear-sync have no archive/delete-issue mutation → "clear cancelled cards" needs UI archive or the label+Done workaround; big list_issues → jq + cursor. | memory (linear-ops-archive-limit) | promoted |
| 2026-06-27 | CAM-205 | orchestrator | process | Created a Linear story for nearly every task incl internal tooling (skill-builder/seo), while doc/process PRs correctly had none → over-carded /status. → Card only deliverable work (feature/fix/perf/security with AC, gated); tooling/docs/config/process = plain PR, no card. | `.claude/rules/discovery.md` | promoted |
| 2026-06-27 | CAM-197 | orchestrator | process | Closed stories on "merged + key signal ok" / a bare owner "ปิด" without an AC-row-by-row check (visual ACs unverified). → Before Done, walk every AC row (verified/owner-verify/N-A); visual/browser-only rows need a structured owner sign-off. | `.claude/rules/qa.md` | promoted |
| 2026-06-27 | CAM-186 | orchestrator | process | Executed owner instructions too eagerly ("promote ทั้งหมด" 7 rows; rapid skill creation; reflexive cards) instead of questioning. → Doubt + ask back before acting when unclear / risky-irreversible / a better path exists / it conflicts with a standard; just do clear low-risk asks. | memory (orchestrator) | promoted |
| 2026-06-27 | CAM-199 | devops | process | Branched off `staging` while another team's uncommitted WIP (CAM-198) sat in the tree → it rode onto the new branch twice; a `git add -A` would have committed it. → Pre-flight `git status` before branching; stage **explicit paths**, never `git add -A` when the tree may hold others' WIP (same root cause as CAM-201's concurrent-tree collisions). | `.claude/rules/ops.md` | promoted |
| 2026-06-27 | CAM-203 | devops | process | `gh pr merge … && echo merged` printed "merged" but the PR stayed **OPEN** — branch BEHIND after a concurrent merge → required `quality-gate` went "expected" → even `--admin` was blocked. → Confirm a merge with `gh pr view --json state`; if BEHIND, merge base in → re-run CI → merge. Never trust the echo. | `.claude/rules/ops.md` | promoted |
| 2026-06-27 | CAM-200 | frontend | perf | `import * as X from 'lucide-react'` shipped the full 1414-icon barrel = 173 KB (53% of Home First-Load JS). → A wildcard/barrel import pulls the whole library into the client bundle; use named imports; confirm the heavy chunk with `ANALYZE=1 build` before guessing the cause. | `.claude/rules/performance.md` | promoted |
| 2026-06-27 | CAM-199 | frontend | image | `next/image` lazy-loads by default, so the LCP/above-the-fold image stayed `loading="lazy"` (2,040 ms resource-load-delay, LCP stuck 5.5s) even after PERF-4. → Set `priority` on the LCP/above-the-fold image (first ~N cards only, never all). | `.claude/rules/performance.md` | promoted |
| 2026-06-27 | CAM-202 | security | security | A `default-src 'self'` CSP silently breaks real third-party origins the app uses (Leaflet→`*.tile.openstreetmap.org`, image CDNs, fonts). → Inventory the app's external origins before writing/tightening a CSP; when breakage is browser-only, ship the enforced policy as **Report-Only first**, flip after the console is clean (CAM-203). | `.claude/rules/security.md` | promoted |
| 2026-06-27 | CAM-203 | security | security | Restructuring NextAuth v5 middleware to the `auth((req)=>…)` form (to inject a CSP nonce) dropped the auto-invoked `authorized` callback → `/dashboard` nearly shipped unprotected. → Re-verify route protection with a test after ANY auth/middleware restructure (an authz regression is worse than the bug being fixed). | `.claude/rules/security.md` | promoted |
| 2026-06-27 | CAM-199 | qa | process | Lighthouse CWV (LCP/TBT), CSP console violations, skeleton-on-click, dark-theme visual are **browser-only** — not verifiable headless. → Mark them owner-verify ACs; the agent verifies the curl-able layer (HTTP status, headers, SSR HTML, asset 200, redirect) and never claims a browser-only AC Done from curl alone. | `.claude/rules/qa.md` | promoted |
| 2026-06-27 | CAM-201 | code | process | "Works on local, breaks on Staging/Prod" was chased as app logic for 3 rounds. → For an environment-specific bug, suspect env differences/HTTP cache headers/case-sensitivity (macOS→Linux) **before** app logic. | `.claude/rules/code.md` | promoted |
| 2026-06-27 | CAM-201 | performance | perf | Walk-sprite frames swapped via `background-image` flickered only on deployed because `/public` defaults to `Cache-Control: max-age=0, must-revalidate` → a network revalidation per frame. → Runtime-swapped static assets need an **immutable** long cache; preload+decode frames that animate. | `.claude/rules/performance.md` | promoted |
| 2026-06-27 | CAM-201 | qa | process | The v0.10.1 guard tests were source-inspection only, so they passed while the bug persisted. → A guard test must **fail on the real defect**; prove teeth (remove the fix → test goes red) and prefer a behavioral/measurable check (e.g. the deployed asset's header) over source-grep. | `.claude/rules/qa.md` | promoted |
| 2026-06-27 | CAM-201 | devops | process | Two sessions ran git in the same working tree → branch/tree collisions; Vercel also deduped a staging deploy when the merge tree equaled a branch-preview tree. → Isolate concurrent work in a `git worktree`; force a tree-changing commit when a staging deploy dedups. | memory (project-status, one-off) | proposed |

| 2026-07-05 | workflow | devops | process | Hit the Vercel Hobby 100-deployments/day rolling limit twice in two days; ~70-75% of quota was preview deploys of branches nobody opens. Official docs: builds skipped via the Ignored Build Step STILL count as full deployments; staging is preview-class so a blanket preview kill would take it down too. → Quota control = `vercel.json` `git.deploymentEnabled` allow-list (`staging`/`main` only); the ignore script only saves build minutes. | `.claude/rules/ops.md` | promoted |
| 2026-07-05 | workflow | devops | process | Post-deploy smoke had silently self-skipped since setup — repo variables `STAGING_URL`/`PROD_URL` were never created, and the job exits 0 with a notice when the var is unset. → A conditional CI job needs a probe that proves it RAN at least once (found while building the env config matrix). | `.claude/ENV-CONFIG.md` | promoted |
| 2026-07-05 | CAM-355 | architect | process | The spec's enforcement-reader inventory listed 4 readers and missed the 5th (`/availability` calendar route feeding the camper date-picker) — surfaced as a G3 Important + same-PR rework. → Derivation/enforcement changes require a grep-backed reader/writer inventory tagged NOW/LATER/NO-CHANGE (architecture.md 15b). | `.claude/rules/architecture.md` | promoted |

| 2026-07-05 | CAM-341/360 | backend | data | `payload.x \|\| undefined` collapsed clear-intent into an omitted key TWICE (fee/policy, then logo) because the explicit-null fix was re-derived per field. → One shared nullable-clear pattern for every clearable optional field. | `.claude/rules/api.md` | promoted |
| 2026-07-05 | CAM-353/357 | architect | perf | `unstable_cache` fixes tags at wrap time — module-level wrapper with static tags made every `revalidateTag()` inert since CAM-195, masked by TTL. → Per-call wrapper when tags depend on runtime args + behavioral invalidation test. | `.claude/rules/architecture.md` | promoted |
| 2026-07-05 | CAM-348/349/352 | architect | data | Shape/relation changes (images CSV→relation) and hard→soft-delete conversions broke consumers one-by-one as the owner found them. → 15b inventory sweep extended to shape changes and delete-semantics conversions. | `.claude/rules/architecture.md` 15b | promoted |
| 2026-07-05 | CAM-359 | frontend | data | e2e "flake" was a real last-write-wins race: Strict Mode double-mount + post-create refetch with no request sequencing silently erased fresh data. → Monotonic requestId gate on every concurrent async load. | `.claude/rules/code.md` | promoted |
| 2026-07-05 | CAM-351 | frontend | data | `sum \|\| fallback` treats derived 0 as "no value" and leaks the stale fallback. → Gate derived-total display on an explicit count, never sum truthiness. | `.claude/rules/code.md` | promoted |
| 2026-07-05 | CAM-362 | frontend | data | One source failing inside a shared Promise.all catch blanked a sibling section that had loaded fine. → Never-throwing safe fetch + scoped error/retry per independent source. | `.claude/rules/code.md` | promoted |
| 2026-07-05 | CAM-344 | security | security | Client-controlled iteration count (8000-year date span) on a public GET ran a synchronous per-night loop to completion; fail-open try/catch bounds errors, not CPU. → Explicit MAX cap before any client-controlled loop. | `.claude/rules/security.md` | promoted |
| 2026-07-05 | CAM-359/db-reset | security | security | "Mask before @" URL redaction leaks query-string secrets into logs; db-reset.mjs still carries the vulnerable pattern its own successor fixed. → hostname-only diagnostics everywhere. | `.claude/rules/security.md` + CAM-369 | promoted |
| 2026-07-05 | CAM-358 | qa | security | Widening a validator shipped with accept-tests only; the scheme-rejection invariant had zero direct tests until a G3 nit. → Widening = add reject-tests for the negative space in the same change. | `.claude/rules/qa.md` | promoted |
| 2026-07-05 | CAM-359/346 | qa | process | Two "flakes" this period were real defects (a product race; a structural stream-drain race). → A flaky test is a defect report until root-caused; retry/quarantine only after. | `.claude/rules/qa.md` | promoted |
| 2026-07-05 | CAM-261 | qa | a11y | Verifying contrast from the ticket's example list missed 4 same-pattern violations; the real axe scan against the BUILT app found them. → The measurable gate is the scope, not the example list. | `.claude/rules/qa.md` | promoted |
| 2026-07-05 | CAM-305 | frontend | data | `res.json() as T` skips runtime validation — an off-contract 200 crashes at render instead of reaching the documented error state. → Validate fetched shapes; a cast is not a check. | `.claude/rules/code.md` | promoted |
| 2026-07-05 | CAM-368 | designer | a11y | PanoramaViewer copied ImageGallery's chrome and silently inherited its missing focus-trap/scroll-lock — behavioral a11y gaps clone just like visual drift. → Check for/extract a shared a11y hook before cloning modal chrome. | `DESIGN.md` | promoted |
| 2026-07-05 | PR390/392 | orchestrator | process | A flow/policy rewrite updated ops.md but not CLAUDE.md's restatement; the contradiction rode through a promote until a G3 incidentally caught it. → Policy change = grep every SoT restatement in the same PR. | `.claude/rules/ops.md` | promoted |
| 2026-07-05 | PR390 | devops | process | ci.yml `pull_request.branches` and `push.branches` edited separately would strand PRs into a new protected branch with no required check ever reporting. → Edit both trigger lists together, always. | `.claude/rules/ops.md` | promoted |
| 2026-07-05 | smoke/adhoc | devops | process | Conditional CI jobs that exit 0 with a notice when a var/secret is missing self-skip silently for months (smoke did; camper-adhoc/gate-continue share the shape). → Every conditional job needs a first-run proof. | `.claude/rules/ops.md` | promoted |
| 2026-07-05 | CAM-368 | orchestrator | process | Builder's first git commands ran in the MAIN tree (the owner's live dev server) before self-correcting. → Builders verify pwd = assigned worktree before the first git command. | `.claude/agents/*` dispatch contract | promoted |

| 2026-07-28 | CAM-587/596 | backend, qa | process | Shipped district search that the model never invoked — 15 unit tests green AND a real-model guardrail gate green, because every test supplied the arguments and no guardrail case covered the new parameter. → A real-model gate is green only over the cases it contains; adding a tool parameter adds a golden case that asserts the MODEL sets it (`strictParams`), or the gate passes on an untested surface. | `.claude/rules/qa.md` (strengthen the CAM-500 row) | promoted |
| 2026-07-28 | CAM-596 | backend, architect | process | The tool description told the model "if you do not recognise the district, leave it unset and use keyword" — the model cannot recognise แม่ริม; only AdminArea can. It obeyed and returned nothing. → Never ask the model to decide what only the data layer knows; resolve deterministically server-side and pin the result. | `.claude/rules/architecture.md` | promoted |
| 2026-07-28 | CAM-595 | backend | perf | `take: 500` + `orderBy: number asc` truncated the NEWEST rows — the work in flight — and said nothing, so `gates` printed "no gates open" while five stories waited; a story sat invisible for two days. → A bound must order so truncation drops the least-relevant rows, and must emit a truncation signal; "bounded" without either is a silent wrong answer. | `.claude/rules/performance.md` (strengthen the unbounded-fetch bullet) | promoted |
| 2026-07-28 | CAM-595/602 | backend | process | A server that predates a new query param ignores it (zod drops unknown keys) and returns the unfiltered set; the client, having correctly stopped filtering locally, reported "0 waiting on you" over 500 wrong rows. → A response must prove which filter/mode was applied; a client that cannot tell "honoured and empty" from "ignored and everything" must refuse to interpret. | `.claude/rules/api.md` | promoted |
| 2026-07-28 | CAM-598 | frontend | process | The dispatch named the flexbox `min-w-0` truncation trap as the cause; the agent measured at 320/390px, found Thai script never triggers it, and left working code untouched. → A named mechanism from the orchestrator is a hypothesis, not a finding; measure before applying a known idiom, and report "already correct" as a result. | ledger only (covered by performance.md Measure→Fix) | proposed |
| 2026-07-28 | retro | orchestrator | process | Ran a "before/after" comparison of `resolvePlace` across two trees and got identical output — the Bash tool's cwd persists between calls, so both runs executed inside the same worktree. Nearly reported a false regression off that baseline. → A before/after across trees passes absolute paths or re-asserts cwd; a baseline that matches the branch under test is a tooling artifact until proven otherwise. | `.claude/agents/*` dispatch contract | proposed |
| 2026-07-28 | CAM-562 | orchestrator | process | Under a blanket owner "อนุมัติทั้งหมด", `complete()` still refused a story whose `roleHistory` held only `backend-engineer` — the Verify-stage guard held against the owner's own approval, and the story was routed through QA properly instead. → Evidence the terminal gate works; record, do not weaken. | ledger only (no rule change) | proposed |
| 2026-07-28 | 9 stories | orchestrator | process | Used the `approve` verb (clears a gate, returns to IN_PROGRESS) on all nine when the owner's "approve everything" meant the TERMINAL gate (`--state Done` → `complete`), then had to re-raise and re-complete each. → At the last gate, owner approval means complete, not approve; the two verbs are not interchangeable. | `.claude/commands/camper.md` (one-line clarification) | promoted 2026-08-06 |

| 2026-08-06 | CAM-687 | devops, orchestrator | process | `npm update` inside an agent worktree wrote THROUGH the shared node_modules symlinks into the MAIN tree — the owner's live dev server — leaving ~150 real packages empty mid-session; `worktree-setup.sh` isolates only `.prisma`/`@prisma`, nothing defends against dependency installs. → Dependency work runs where node_modules is real; a tree repair is verified package-by-package, never inferred from a clean `git status`. | `.claude/rules/ops.md` | promoted |
| 2026-08-06 | CAM-680 | orchestrator | process | Marked the story Done in the SAME command batch as the merge; PR #760 had gone BEHIND when #759 landed, the merge was refused, and the board read Done over code that was not on `dev` — by the same actor who had been citing the CAM-504 rule all session. → Knowing a rule does not enforce it; merge, confirm `state=MERGED`, and close are three separate calls. | `.claude/rules/ops.md` (strengthened the CAM-504 row) | promoted |
| 2026-08-06 | CAM-666/672 | orchestrator | process | Reported a deployed change as verified because a curl of the served HTML contained the price headline — the match was host-authored `feeInfo` prose. Separately, an HTTP 200 cannot date a deploy: the old build answers 200 too. → Probe a structural anchor that exists ONLY in the new build and assert the discriminating value. | `.claude/rules/ops.md` | promoted |
| 2026-08-06 | CAM-670 | orchestrator | process | The dispatch's `done_when` demanded a code shape (`grep onDelete: SetNull`) that Prisma cannot emit without nulling a NOT NULL column; the agent tested the instruction against real Postgres, refuted it, and shipped a column-scoped raw FK. → `done_when` asserts the outcome a command's output proves; a code-shape criterion is a hypothesis the agent may refute. | `.claude/rules/efficiency.md` + orchestrator §Dispatch contract | promoted |
| 2026-08-06 | CAM-664/671 | qa | process | A `test.fail()` guard documenting a live defect INVERTED the moment the defect was fixed — Playwright reports "expected to fail, but passed" as a failure, inside a file the fixer never touched, costing a CI round mid-epic. → A `test.fail()` guard names the ticket that will invert it, and that ticket carries the flip in its own scope. | `.claude/rules/qa.md` | promoted |
| 2026-08-06 | CAM-235/661 | frontend, designer | process | Restored Dialog's `backdrop-blur` at `-md`; CAM-235 had removed Dialog blur DELIBERATELY for modal open/close GPU cost. → Chesterton's Fence runs both ways — the absence of a property can be a decision; `git log -S` for the ticket that removed it, restore at or below that cost, supersede its test pin with a dated note rather than deleting it. | `.claude/rules/code.md` | promoted |
| 2026-08-06 | CAM-663 | backend | data | A demo/seed script overwrote real `Spot` rows having recorded nothing, then hardcoded a capacity-moving flag in its `--undo`, and named demo pitches `CAM-663 Demo — …` on a camper-facing screen. Its fake-Prisma unit suite could not see any of it. → Data scripts are create-only over real data; undo restores RECORDED state; the record lives outside the script's own artifacts; written content is indistinguishable from real. | `.claude/rules/api.md` | promoted |
| 2026-08-06 | CAM-685 | security | security | Hardcoded `https://campvibe.app` as the origin of a clickable deep link in an outgoing host email, reasoning from the same domain already being hardcoded as the default `EMAIL_FROM`. → An unverified SENDER merely fails to send; a LINK origin we may not control sends the recipient elsewhere, permanently and unrecallably. Outbound origins read from config; an existing hardcode is not precedent. | `.claude/rules/security.md` | promoted |
| 2026-08-06 | CAM-686 | architect | process | HostOS appeared to need three unbuilt models because ADR-012 §6 said "every manual stay carries a `leadId`" — one incidental coupling clause written when leads were assumed to come first. Dropping it put the first slice at ZERO new models. → Separate an ADR's decision from its incidental couplings before scheduling work to satisfy it. | `.claude/rules/architecture.md` | promoted |
| 2026-08-06 | CAM-664 | orchestrator | process | Queued a verification story in parallel with stories still editing the same UI surface → three lost CI rounds (a fixture absent from the CI e2e DB, a sibling story changing the sticky bar mid-flight, the `test.fail()` guard inverting). → A verification story sequences AFTER every story that edits its surface; this was a sequencing call, not an agent error. | ledger only (no rule — covered by the qa.md `test.fail` row) | proposed |
| 2026-08-06 | worktrees | devops | process | 97 agent worktrees still registered at 29 GB, each with node_modules symlinked into the main tree — 97 live instances of the CAM-687 hazard. `git merge-base --is-ancestor` reports them all "unmerged" because the repo squash-merges, so ancestor checks cannot drive the cleanup. → Carded as CAM-690; the safe reap key is a confirmed PR `state=MERGED` per branch. | CAM-690 | promoted |

> Seeded 2026-06-27 from CAM-201 as the proof-of-loop. The first two rows were promoted (owner-approved) into the named rules; the qa/devops rows are candidates for the next prune/promote pass.
