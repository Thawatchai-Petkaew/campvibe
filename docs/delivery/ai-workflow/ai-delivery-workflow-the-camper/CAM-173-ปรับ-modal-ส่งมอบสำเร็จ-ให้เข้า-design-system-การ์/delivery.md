---
linear: CAM-173
feature: ai-workflow
epic: ai-delivery-workflow-the-camper
persona: Admin
artifact: delivery
owner: devops-release
status: In Progress
version: v1
updated: 2026-06-25
---
# Delivery — ปรับ modal ส่งมอบสำเร็จ ให้เข้า design-system + card list + entity-decode (CAM-173)

## PR & preview

PR: pending (see commit `feat(status-map): delivery modal to design-system surface + card list + entity-decode (CAM-173)`)
Branch: `feat/cam-173-delivery-modal-polish` → base `staging`
Vercel Preview: auto-generated by Vercel on PR open

## Staging verify

Gate: G3 (merge → staging). Staging verification steps once CI green and merged:

1. Open `campvibe-staging.vercel.app/status/map`
2. Trigger the gift button (a new unseen delivery must exist) — the delivery modal opens
3. Verify: modal uses app design-system surface (bg-popover/foreground tokens, theme-aware — no dark glass overlay)
4. Verify: each delivery renders as a Card with CheckCircle2 icon + decoded title + epic Badge + Thai date
5. Verify: titles containing `&quot;` or other HTML entities display as proper characters (no raw entity strings)
6. Verify: gift button behaviour and map-delivery logic are unchanged

All AC verified on real Staging URL = Done (Linear state `Done`).

## Migration

None — UI-only change. No schema change, no `prisma migrate deploy` required.

## Release

Pending G4 Staging sign-off and G5 go-live decision.

On promote (`staging`→`main`):
- git tag: `v0.8.3` (or next available after current prod baseline v0.8.2)
- Changelog entry: `feat(status-map): CAM-173 — delivery modal redesigned to use design-system surface (bg-popover/Card/Badge) + HTML entity decode`
- Rollback plan: revert the squash-merge commit on `main` (`git revert <merge-commit-sha>`) + redeploy; no DB rollback needed (UI-only, no migration)

## Error watch

Pending (post-deploy to staging). Watch window: 15 minutes post-deploy on Staging.
Thresholds: error rate >= 2x pre-deploy baseline OR > 10% of requests erroring = investigate/rollback.
Prod watch: pending G5.

## Links

`story.md` · `design.md` · `tech.md` · `test.md` · `.claude/rules/ops.md`

## Changelog

- v1 (2026-06-25) — created by DevOps/Release (The Camper); PR open, awaiting CI + G3 merge
