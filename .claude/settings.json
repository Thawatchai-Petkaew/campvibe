{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      { "matcher": "Bash",
        "hooks": [ { "type": "command",
          "command": "case \"$CLAUDE_TOOL_INPUT_command\" in *\"git commit\"*) npx --no-install tsc --noEmit && npm run lint || { echo 'BLOCK: lint/typecheck failed'; exit 2; };; esac" } ] }
    ]
  }
}
